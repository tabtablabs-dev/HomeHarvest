{"id":"HomeHarvest-otu","title":"Fix get_bulk_prop_details causing 403 errors in Realtor scraper","description":"The get_bulk_prop_details method in homeharvest/core/scrapers/realtor/__init__.py is reportedly causing 403 errors.\n\nSuggested Fix:\nThe original implementation constructed a single massive GraphQL query with hundreds of aliases. This is now blocked by WAF.\nReplace it with a parallel execution strategy using individual, parameterized queries.\n\nProposed Implementation:\n    @retry(\n        retry=retry_if_exception_type((JSONDecodeError, Exception))\n        \u0026 retry_if_not_exception_type(AuthenticationError),\n        wait=wait_exponential(multiplier=1, min=1, max=10),\n        stop=stop_after_attempt(3),\n    )\n    def get_bulk_prop_details(self, property_ids: list[str]) -\u003e dict:\n        \"\"\"\n        Fetch extra property details for multiple properties using parameterized GraphQL queries.\n        Returns a map of property_id to its details.\n        \"\"\"\n        if not self.extra_property_data or not property_ids:\n            return {}\n        property_ids = list(set(property_ids))\n        results = {}\n        # Use parameterized query for single property\n        query = (\n            \"\"\"query GetHome($property_id: ID!) {\n            home(property_id: $property_id) %s\n        }\"\"\"\n            % HOMES_DATA\n        )\n        def fetch_one(pid):\n            try:\n                variables = {\"property_id\": pid}\n                data = self._graphql_post(query, variables, \"GetHome\")\n                if data.get(\"data\") and data[\"data\"].get(\"home\"):\n                    return pid, data[\"data\"][\"home\"]\n            except Exception:\n                pass\n            return pid, None\n        with ThreadPoolExecutor(max_workers=self.NUM_PROPERTY_WORKERS) as executor:\n            future_to_pid = {\n                executor.submit(fetch_one, pid): pid for pid in property_ids\n            }\n            for future in as_completed(future_to_pid):\n                pid, result = future.result()\n                if result:\n                    results[pid] = result\n        return results\n","status":"in_progress","priority":2,"issue_type":"bug","created_at":"2025-12-24T15:58:04.797068-06:00","updated_at":"2025-12-24T16:12:15.692159-06:00"}
